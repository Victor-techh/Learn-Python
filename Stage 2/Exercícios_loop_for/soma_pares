#Programa que ler seis números inteiros e mostre a soma apenas daqueles que forem pares.
#Se o valor digitado for ímpar, desconsidere-o.
soma = 0 #usadas para acumular valores
cont = 0 #usada para contar
for c in range(1, 7):
    num = int(input(f'Digite o {c}° valor: '))
    if num % 2 == 0:
        soma += num #acumulador
        cont += 1 #contador
print(f'Você informou {cont} números pares e a soma foi {soma}')
