#Programa que calcule a soma entre todos os números que são múltiplos de três
#e que se encontram no intervalo de 1 até 500.

#variavel que irá armazenar a soma dos números multiplos de 3
soma = 0 
#loop for
print('Os números ímpares entre 1 e 500, e múltitplos de 3:')
for c in range(1, 501, 2):
  if c % 3 == 0:
    print(c)
    soma = soma + c
print(f'\nO total da soma de todos os números múltiplos:\n{soma}')
