#Crie um programa que leia números inteiros pelo teclado. 
#O programa só vai parar quando o usuário digitar o valor 999,
#que é a condição de parada. 
#No final, mostre quantos números foram digitados 
#e qual foi a soma entre elas (desconsiderando o flag).
cont_num = soma = 0
while True: #loop infinito
  num = int(input('Digite um número ou [999 para encerrar]:  ')) #solicitação antes da condicional para que o flag não seja somado. 
  if num == 999:
    break
  cont_num += 1
  soma += num
print('Encerrando o programa!')
print(f'Foram digitados {cont_num} números.')
print(f'A soma dos números digitados é {soma}.')
