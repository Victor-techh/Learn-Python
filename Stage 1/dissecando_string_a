#Programa que recebe uma frase do usuario e mostra: 
#quantas vezes aparece a letra "a", 
#em que posição ela aparece a primeira vez, 
#e em que posição ela aparece a ultima vez.

#variavel que recebe a frase do usuario;
frase = str(input('Digite uma frase: ')).strip().upper()
print(frase)
#vezes aparece a letra "a" na frase;
print('A letra a aparece',frase.count('A'),'vezes')
#mostra em que posição a letra 'a' aparece a #primeira vez;
print('Ela aparece a primeira vez na posição',frase.find('A'))
#mostra em que posição a letra 'a' aparece pela ultima vez;
print('E aparece pela última vez na posição',frase.rfind('A'))
